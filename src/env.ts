import {  Actor, Guid, Material } from "@microsoft/mixed-reality-extension-sdk";
import Thing from "./thing";

export default class AppEnvironment {
    // public paintTargets: Map<Guid,Actor> = new Map<Guid,Actor>();
}